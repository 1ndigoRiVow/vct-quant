# VCT CN 选手股量化预测与自动交易系统 — 产品需求文档（PRD）

| 字段 | 内容 |
|---|---|
| 文档版本 | v1.0（2026-08-12） |
| 产品代号 | vct-quant |
| 仓库 | https://github.com/1ndigoRiVow/vct-quant |
| 当前状态 | Phase 1 完成，Phase 2 待启动 |
| 作者 | 产品/工程：vorwa + Codex（采集器） |
| 下一步 | Phase 2 第二层人气模型 + 真实数据接入 |

---

## 1. 背景与问题

### 1.1 我们在做什么
为 **VCT CN（无畏契约中国赛区）** 构建一个"**虎扑选手股**"量化预测与自动交易策略系统。把每名职业选手当作一支"股票"：
- **股价 = 社区人气**（虎扑赛后评分、讨论热度）
- **内在价值 = 客观比赛实力**（基于 KDA / ACS / 首杀 / 地图胜率等真实数据建模）
- **交易逻辑 = 人气与实力的偏离**：人气被低估则"买入"，被高估则"卖出"

### 1.2 核心洞察（产品的哲学根基）
"选手股"本质上是一个**人气预测问题**，等价于金融里的资产定价：

> 价格（人气水平）不可直接预测，但**价格的漂移（收益率/人气变化）是可预测的**。

类比金融的核心纪律，本产品坚持两条原则：
1. 第一层（价值）客观、平稳、可解释——不随时间剧烈波动；
2. 第二层（人气）不该硬猜"人气值是多少"，而应预测"**人气相对预期会漂移多少**"（条件期望 E[ΔP]）。

这把问题从"猜一个噪声主导的水平"转化为"学一个可解释的反应函数"——这正是双层模型成立的关键。

### 1.3 要解决的痛点
- 虎扑选手人气波动剧烈且非理性（热门选手涨跌放大、冷门选手"爆冷即暴涨、惜败不跌"的凸性），缺乏量化刻画；
- 现有雏形只做"横截面 V\* 与 P 的静态比较"，没有**前瞻**——无法回答"下一场比赛后人气会变多少"；
- 比赛结果对人气的影响函数（惊喜度反应）从未被建模，alpha 潜藏其中。

---

## 2. 目标与范围

### 2.1 目标（In Scope）
- 第一层：从客观比赛数据产出**不随时间剧烈波动的选手综合实力值 V\***，并附**赛前胜率预测 p̂**；
- 第二层：融合 V\* + p̂ + 社区舆情 + 动量，预测**未来一个时段的人气漂移 E[ΔP]**，并给出置信区间；
- 交易层：当预测人气 E[P′] 低于实力 V\* 时发出买入信号，反之为卖出信号；多空组合、半凯利仓位、walk-forward 回测；
- 输出层：可复现的 HTML 信号报告 + Chart.js 仪表盘，支持每日定时运行。

### 2.2 非目标（Out of Scope，本阶段不做）
- **不做**模拟经营 / 战队管理游戏（那是 esports-manager 的定位，本项目只借鉴其引擎思路）；
- **不做**真实资金交易接口（系统输出"信号 + 仓位建议"，执行需人工或半自动网关，且涉及合规）；
- **不做**赛事结果直播 / 赔率对冲盘（仅用 VLR 数据建模，不接入博彩）；
- **不做**重深度学习黑箱（起步用可解释回归 / LightGBM，β 系数必须可读）。

---

## 3. 核心产品概念：双层预测模型

```
                    ┌─────────────────────────────────────────────┐
                    │            真实比赛结果（Y: 0/1 胜）           │
                    └───────────────┬─────────────────────────────┘
                                    │   s = Y − p̂   (惊喜度)
  ① 第一层：选手实力（内在价值）      ▼
  ──────────────────────────────   ② 第二层：社区人气（当前定价）
  输入：KDA/首杀/阵容/地图胜率       ──────────────────────────────
  输出：V*（实力估值）               输入：V* + p̂ + 舆情 + 动量 + FK/FD
        p̂（赛前胜率，Glicko-2 推导） 输出：E[ΔP]（人气漂移条件期望）+ CI
  特性：客观 / 平稳 / 可解释                    P′ = P + E[ΔP]
                                    │
                                    ▼
                        交易信号： E[P′] < V* → 买入（洼地）
                                 E[P′] > V* → 卖出（泡沫）
```

### 3.1 第一层：选手实力评分（客观、内在价值）
- **输入**：VLR.gg 客观比赛数据（KDA、首杀 FK、首死 FD、ACS、ADR、特工池、地图胜率、阵容）；
- **输出**：
  - `V*`：选手综合实力值（Glicko-2 评分体系，天然给出 rating / rd / vol，rd 表征不确定性）；
  - `p̂`：给定对阵双方的赛前胜率（FPS 回合制蒙特卡洛模拟器推导）。
- **特性**：不随时间剧烈波动（Glicko-2 本身带均值回归）、客观、可解释——量化选手真实比赛影响力。

### 3.2 第二层：社区人气值预测（主观、当前定价）
- **输入**：第一层的 `V*` + `p̂` + 虎扑舆情（赛后评分 `rating_avg`/`rating_count`、评论情感）+ 近期比赛动量因子；
- **输出**：未来一个时段（如一场比赛后）的**人气漂移条件期望 `E[ΔP]`**（含置信区间）；
- **反应函数（可学目标）**：
  ```
  ΔP = α(热度) + β₁·s + β₂·动量 + κ·(P − V*) + ε
   s = 实际结果 − 赛前预测概率 p̂               （惊喜度，由第一层定义）
  ```
  - `α(热度)`：热门选手波动放大（你观察到的现象①）；
  - `β₁`：对"惊喜度"的敏感度，体现凸性——冷门选手爆冷（s 为正且 |s| 大）大涨、惜败（s 为负但 |s| 小）不跌（现象②）；
  - `κ·(P−V*)`：现有静态横截面项，在第二层里作为**输入**而非结论，从"静态价值投资"升级为"动态预期修正"；
  - `ε`：承认人气存在不可预测随机噪声，模型输出置信区间，信号只在 E[ΔP] 显著时触发。

### 3.3 交易信号
- `E[P′] = P + E[ΔP]`
- `E[P′] < V*` → **买入**（人气被低估，看涨，按 A 股惯例标红）
- `E[P′] > V*` → **卖出**（人气被高估，看跌，标绿）
- 仓位：半凯利（KELLY_FRACTION=0.5）× 信号强度；组合 dollar-neutral（多头空头净敞口 ≈ 0）。

---

## 4. 用户与角色

| 角色 | 描述 | 关注点 |
|---|---|---|
| 量化研究员（你） | 设计模型、调参、回测、判定上线 | β 系数、校准指标、回测净值/回撤 |
| 数据采集 Agent（Codex） | 维护虎扑/VLR 爬虫与舆情采集 | 数据可得性、反爬、增量幂等 |
| 系统/调度 | 每日定时跑全管线、出日报 | 稳定性、可重入、零人工干预 |
| （未来）订阅用户 | 看信号面板、跟单建议 | 信号列表、胜率、风险提示 |

---

## 5. 系统架构

### 5.1 分层架构（已落地）
```
采集层  collectors/   VLR.gg 比赛/选手统计 + 虎扑/贴吧舆情 + 虎扑赛后评分(Codex)
   ↓
存储层  storage/      SQLite（11 张表）+ repo 仓储层（幂等 upsert）
   ↓
特征层  features/     滚动实力窗口 + 情绪因子(BERT/LLM) + 动量
   ↓
建模层  models/       Glicko-2 评分 + FPS 蒙特卡洛模拟器(p̂) + 选手画像 + 价值残差
   ↓
策略层  strategy/     多空信号 + 半凯利仓位 + dollar-neutral 组合
   ↓
输出层  reporting/    HTML 信号报告 + Chart.js 仪表盘
```
调度入口：`vct_quant/main.py`（离线/在线双模式，`OFFLINE_MODE` 切换）。

### 5.2 已建模块 vs 待建模块

| 模块 | 状态 | 说明 |
|---|---|---|
| VLR 比赛/选手统计采集 | ✅ 已实现 | 离线合成 + 在线解析框架；`player_stats` 已含 rounds |
| 虎扑/贴吧舆情采集 | ✅ 已实现 | 离线合成；在线需 requests+bs4 |
| 虎扑赛后评分 + 评论采集 | ✅ Codex 交付 | `HupuRatingsCollector`（双模式）+ 手动 CSV 导入器；已入库 |
| Glicko-2 实力评分 | ✅ 已实现 | `player_ratings` |
| 选手多维画像 | ✅ 已实现 | `player_profiles`（rounds/kda/adr/acs/fk/fd/主位置） |
| 选手-战队映射 | ✅ 已实现 | `player_teams`（近 5 场推断） |
| 战队地图胜率 | ✅ 已实现 | `team_map_winrate`（team×map×opponent） |
| FPS 蒙特卡洛模拟器（p̂） | ✅ 已实现 | `match_simulator.py`；校准 Brier/LogLoss/命中率 |
| 第二层人气模型（E[ΔP]） | ⏳ Phase 2 | 反应函数回归/LightGBM，待真实数据 |
| 真实数据接入（OFFLINE_MODE=False） | ⏳ 阻塞于网络/凭据 | 需本机可联网 + 虎扑 Cookie |
| 信号整合 + walk-forward 回测 | ⏳ Phase 3 | 用 E[P′] 替代/增强现有静态 Δ |
| 定时调度 | ⏳ 待定 | 每日自动采集→预测→日报 |

---

## 6. 数据模型（现有 11 张表）

**比赛/统计**：`matches`、`maps`、`player_stats`（含 `rounds` 列）
**舆情**：`posts`、`sentiments`
**实力/信号**：`player_ratings`、`value_signals`
**画像/映射/胜率**：`player_profiles`、`player_teams`、`team_map_winrate`
**虎扑**：`hupu_match_ratings`（rating_avg/rating_count 热度代理）、`hupu_comments`

> 详细字段见 `vct_quant/storage/schema.sql`。数据写入一律走 `repo.upsert_*`（幂等，`ON CONFLICT`）。

---

## 7. 功能需求（FR）

### FR-1 数据采集
- FR-1.1 VLR.gg 采集比赛、地图比分、每名选手逐图统计（acs/kda/fk/fd/adr/agent/rounds）。
- FR-1.2 虎扑赛后评分采集：每名选手 `rating_avg` + `rating_count`（评分人数=热度代理），按 `match_id` 关联。
- FR-1.3 虎扑赛后评论采集：楼层/作者/内容/点赞/时间，供情感分析。
- FR-1.4 舆情（虎扑/贴吧）采集：帖子文本 + 时间 + 热度，BERT/LLM 情感打分。
- FR-1.5 增量幂等：同一 `match_id` 重跑不重复入库；离线合成模式全管线可零联网跑通。

### FR-2 第一层建模
- FR-2.1 Glicko-2 评分：`rate_all_players` 输出 `rating/rd/vol`，覆盖所有出场选手。
- FR-2.2 赛前胜率 `p̂`：`match_simulator.simulate_match` 蒙特卡洛 N=2000 次，输出 A 队胜率 + 95% 置信区间。
- FR-2.3 校准：`evaluate_calibration` 输出 Brier / LogLoss / 胜负命中率 + 每场明细；真实数据下改用 walk-forward 做 out-of-sample 校准。

### FR-3 第二层建模（Phase 2）
- FR-3.1 配对样本构造：每场比赛 `(赛前特征, 赛后人气变化)`，标签 = `Δrating_avg` 与/或 `Δrating_count`（相对该选手历史基线）。
- FR-3.2 特征工程：`s = Y − p̂`（惊喜度）、近期动量（近 N 场 KDA/ACS 趋势）、热度代理（rating_count 分位）、`P−V*` 静态偏离。
- FR-3.3 反应函数估计：起步用 **事件研究回归（OLS）**，可解释 β；样本充足升级 **LightGBM**（保留特征重要性）。
- FR-3.4 输出 `E[ΔP]` + 置信区间；只在置信区间不跨 0 且幅度超阈值时触发信号。

### FR-4 交易策略
- FR-4.1 信号：`E[P′] = P + E[ΔP]` 对比 `V*`；BUY/SELL/HOLD。
- FR-4.2 仓位：半凯利 `KELLY_FRACTION=0.5` × 信号强度；组合 dollar-neutral（`MAX_POSITION_PCT=0.15`、最大回撤 `MAX_DRAWDOWN_PCT=0.20`）。
- FR-4.3 回测：walk-forward（`WALKFORWARD_TRAIN_DAYS=90`），输出净值、命中率、最大回撤。

### FR-5 输出
- FR-5.1 HTML 信号报告：信号明细、选手画像、战队地图胜率、p̂ 校准板块。
- FR-5.2 Chart.js 仪表盘：V\* vs P 散点、Δ 残差条、位置分布、ACS 排行、地图胜率 TOP、校准散点。
- FR-5.3 可重入：每日重跑不清空结构，仅 upsert 当日数据。

---

## 8. 实施路线（含已完成）

| Phase | 内容 | 状态 | 关键产出 |
|---|---|---|---|
| Phase 0 | 数据可行性确认 | ✅ | 确认虎扑有赛后评分+评论；Codex 采集器规格已写 |
| Phase 1 | 第一层补全 p̂ | ✅ 已完成 | `match_simulator.py`；离线校准 n=24, Brier=0.242, 命中率 75%（合成数据仅验证逻辑） |
| **Phase 2** | **第二层人气模型 E[ΔP]** | **⏳ 明天启动** | **反应函数回归/LightGBM；需真实样本 ≥30-50 场** |
| Phase 3 | 信号整合 + walk-forward 回测 | ⏳ 待定 | 用 E[P′] 替换静态 Δ；净值/回撤对比 |
| Phase 4 | 真实数据常态化 + 定时调度 | ⏳ 待定 | OFFLINE_MODE=False 长期跑；每日日报 |

---

## 9. 成功指标 / 验收标准

- **第一层校准**：真实数据下 Brier < 0.25（优于随机）、LogLoss 收敛、胜负命中率 > 60%（walk-forward）。
- **第二层模型**：反应函数 β₁/β₂ 符号合理且显著；样本外 `E[ΔP]` 对真实 `Δrating` 的 R² > 0 且有经济意义。
- **策略回测**：接入真实数据后，净值显著优于买入持有 / 随机；最大回撤可控（< 30%）；信号命中率 > 静态 Δ 基线（当前 19.6% 偏低，需真实数据校准情绪定价）。
- **系统**：`python -m vct_quant.main` 离线零失败；真实模式每日可重入；报告/仪表盘板块完整渲染。

---

## 10. 风险与缓解

| 风险 | 等级 | 缓解 |
|---|---|---|
| 虎扑评分历史不可回溯 / 需登录 | 高 | Codex 采集器已支持匿名优先 + `HUPU_COOKIE` 环境变量 + 降级返回；评分是定格数据可回溯，评论可降级 |
| 模型自噬（越准 E[P′] 越收敛 V\*，信号越少） | 中 | alpha 残留在系统性偏差（颜值/主播溢价、实力型折价、冷门凸性）——这些是模型学不会抹平的，重点挖掘 |
| 样本量小（VCT CN 比赛少） | 中 | 先做可解释回归 + 贝叶斯先验；跨赛区/跨赛季迁移；walk-forward 避免前视偏差 |
| 合成数据指标误导（如命中率 75% 含运气） | 中 | 明确标注"仅验证逻辑"；真实数据接入后重测所有指标 |
| 选手名对齐（VLR vs 虎扑昵称不一致） | 低 | 匹配不上原样落库，后续规则/人工对齐，不卡采集 |
| 真实数据接入网络阻塞（本环境 github:443 不通，已用 SSH 推；VLR/虎扑联网需另行验证） | 中 | 抓取下放沙箱 / 代理；或本机跑采集、推库 |

---

## 11. 开放问题（待决策）

1. **第二层标签用哪个？** `Δrating_avg`（评分均值变化）还是 `Δrating_count`（热度变化），或两者融合？（建议先双跑对比）
2. **真实数据从哪台机器跑？** 本环境 outbound 受限，是否在你另一台设备跑采集、推库回 GitHub？
3. **信号执行边界**：是否接真实"交易"？合规与资金风险如何隔离？（当前定为信号+建议，不接资金）
4. **跨赛事泛化**：样本不足时是否引入 VCT 国际赛 / 其他赛区做迁移学习？
5. **叙事模板（VCT 版）**：esports-manager 的 narration 模块是否本期移植，用于生成"盘面点评"喂情绪因子？

---

## 12. 术语表

| 术语 | 含义 |
|---|---|
| V\* | 第一层输出的选手综合实力估值（Glicko-2） |
| p̂ | 赛前胜率预测（蒙特卡洛模拟器） |
| P | 社区当前人气定价（虎扑评分/热度） |
| E[ΔP] | 第二层预测的人气漂移条件期望（核心可学目标） |
| s | 惊喜度 = 实际结果 − p̂ |
| Δ | 静态残差 = P − V\*（现有系统信号源） |
| 选手股 | 把选手当股票，人气=股价、实力=内在价值 |
| 凸性 | 冷门选手"赢则大涨、输则不跌"的非对称反应 |
| 模型自噬 | 模型越准、定价越有效、套利空间越小 |

---

## 13. 附录：Phase 2 开工任务清单（给 Codex/自己）

- [ ] 构造配对样本：`(match_id, player, 赛前 p̂, V*, 动量, 热度, 赛后 Δrating)`；
- [ ] 写 `models/popularity_model.py`：`fit(X, y)` 估计反应函数，`predict` 输出 `E[ΔP]` + CI；
- [ ] 起步 OLS 事件研究回归，打印 β₁/β₂/κ 系数与显著性；
- [ ] 接 `main.py` 为 `[4.7/8]` 步骤，报告新增"人气漂移预测"板块；
- [ ] walk-forward 校准（避免前视偏差），对比静态 Δ 基线；
- [ ] 真实数据 ≥30 场后再估计，合成阶段只验证管线连通。
